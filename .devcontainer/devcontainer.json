{
  "name": "PosePipe Dev Container",
  "dockerComposeFile": ["docker-compose.yaml"],
  "service": "posepipe", 
  "workspaceFolder": "/workspace",
  // "postCreateCommand": "docker compose -f datajoint_docker/docker-compose-db.yaml",
  // "onCreateCommand": "docker compose -f datajoint_docker/docker-compose-db.yaml",
  // "shutdownAction": "stopCompose",
  // "runArgs": ["-it","--runtime=nvidia","-e","NVIDIA_VISIBLE_DEVICES=5","-v=/datajoint_external:/datajoint_external"],
  "forwardPorts": [
		3306
	],
  "customizations": {

    "settings": {
			"python.pythonPath": "/usr/local/bin/python"
		},
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-toolsai.jupyter"
			]
		}

  }
}